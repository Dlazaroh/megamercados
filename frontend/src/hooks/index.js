/**
 * MegaMercados - Hook useAuth (re-export centralizado)
 * Permite importar desde hooks/ además de context/
 */
export { useAuth } from '../context/AuthContext';
export { useCarrito } from '../context/CarritoContext';
